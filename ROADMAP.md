# Roadmap

- Add OpenAPI security scheme -> MCP auth policy mapping
- Add GraphQL introspection endpoint ingestion
- Add JSON Schema validation generation with zod runtime checks
- Add OpenTelemetry spans in generated server handlers
- Add plugin hooks for custom auth middlewares
- Publish ready-to-run Docker images for generated servers
